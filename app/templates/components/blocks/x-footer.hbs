<div class="container">
  <div class="content has-text-centered">
    <p>
      &copy; {{year}} Safely by Associated Employers.
    </p>
    {{#if auth.authenticated}}
      <p>
        <strong>Logged in as {{auth.user.name.first}} {{auth.user.name.last}}</strong>
        <a href="#" {{action "logout"}}>Logout</a>
      </p>
    {{/if}}
  </div>
</div>
